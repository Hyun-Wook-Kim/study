export default function SearchQuery() {

    this.query = '';
    
    const memoQuery = (searchQuery) => {
        console.log(query)
        query = searchQuery;
    }
    
    return this.query;

}